<?xml version="1.0" encoding="utf-8"?>
<!-- Path: android/app/src/main/res/xml/file_paths.xml -->
<paths>
    <!-- Internal app files -->
    <files-path
        name="internal_files"
        path="." />

    <!-- External storage files -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- Cache directory -->
    <cache-path
        name="internal_cache"
        path="." />

    <!-- External cache directory -->
    <external-cache-path
        name="external_cache"
        path="." />

    <!-- External storage (for older Android versions) -->
    <external-path
        name="external_storage"
        path="." />
</paths>